---
const { title, price } = Astro.props;
---

<div
  class="bg-white border w-full justify-start shadow-sm h-40 rounded-md flex flex-col items-center"
>
  <div
    class="bg-black p-8 text-white w-full h-8 flex items-center justify-center"
  >
    <h1 class="text-2xl">{title}</h1>
  </div>
  <h1 class="mt-8">{price}</h1>
</div>
